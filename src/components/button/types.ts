export enum TypeButton {
  SECONDARY = 'secondary',
  PRIMARY = 'primary',
}

export enum HtmlTypeButton {
  BUTTON = 'button',
  SUBMIT = 'submit',
  RESET = 'reset',
}

export enum SizeButton {
  SMALL = 'small',
  MEDIUM = 'medium',
  LARGE = 'large',
}
